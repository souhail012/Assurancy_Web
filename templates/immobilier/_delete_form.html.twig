<form method="post" action="{{ path('app_immobilier_delete', {'id_fiscal': immobilier.id_fiscal}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ immobilier.id_fiscal) }}">
    <button class="btn">Delete</button>
</form>
