{% extends 'base.html.twig' %}

{% block title %}DevisItem{% endblock %}

{% block body %}
    <h1>DevisItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devis_item.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ devis_item.designation }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ devis_item.quantite }}</td>
            </tr>
            <tr>
                <th>Prix_u</th>
                <td>{{ devis_item.prixU }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ devis_item.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_item_index') }}">back to list</a>

    <a href="{{ path('app_devis_item_edit', {'id': devis_item.id}) }}">edit</a>

    {{ include('devis_item/_delete_form.html.twig') }}
{% endblock %}
